# EMA-stack 1H モード + M15/1H タブ Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> ⚠️ **コミット/プッシュはユーザ明示指示時のみ。** Commit ステップは go サインまで実行しない。
> ⚠️ **Python 実体**: `C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe`。スクリプト出力は ASCII のみ。

**Goal:** 中央 EMA-stack オシレーターに 1H モード(EMA20/EMA80/EMA480, center=480)を追加し、M15/1H タブで切替。周期・中心線以外は M15 と完全同一(過伸張 p95/p99 着色含む)。

**Architecture:** 両モードをサーバで毎サイクル計算しライブ snapshot に同梱(`ema_stack`=M15 / `ema_stack_h1`=H1)。深掘り履歴は `/api/ema_history?tf=` でモード別。H1 過伸張バンドは 16Y Hourly CSV から生成し JSON を `modes` 構造化。フロントはタブで系列・ラベル・bands を切替。単一系列・因果EMA・確定足のみ＝リペイント無を両モードで維持。

**Tech Stack:** Python 3.14 / numpy / pandas / pytest / Flask + flask-sock / vanilla JS + CSS。

**Spec:** `docs/superpowers/specs/2026-06-06-ema-1h-mode-design.md`

---

## File Structure
- Modify: `config.py` — `EmaStackMode` dataclass + `EMA_STACK_MODES` + `EMA_STACK_MODE_BY_NAME`。
- Modify: `analyzer/disparity_bands.py` — `load_bands(mode)` + JSON `modes` 構造。
- Modify: `scripts/gen_ema_disparity_bands.py` — M15+H1 両モード生成。
- Modify: `data/ema_disparity_bands.json` — `modes` 化(再生成)。
- Modify: `analyzer/ema_stack.py` — `EmaStackSnapshot.mode` + `compute_ema_stack` 引数化 + `compute_ema_stack_for`。
- Modify: `analyzer/state.py` — `_ema_stacks` dict + `set_ema_stack` mode キー化 + snapshot 両 stack。
- Modify: `dashboard/serialize.py` — `"mode"` 出力。
- Modify: `analyzer/analysis_loop.py` — 両モード計算。
- Modify: `dashboard/lite_server.py` — `/api/ema_history?tf=`。
- Modify: `static/app.js`, `static/app.css` — タブ + 動的ラベル/キー + モード別履歴キャッシュ。
- Test: `tests/test_ema_modes.py`(新規) + `test_disparity_bands.py` / `test_ema_disparity_bands_artifact.py` / `test_ema_stack.py` / `test_state_and_serialize.py` 追記。

---

## Task 1: config に 2 モード定義

**Files:** Modify `config.py` (after the EMA_STACK_* block, ~line 172); Test `tests/test_ema_modes.py`

- [ ] **Step 1: Write the failing test**

Create `tests/test_ema_modes.py`:

```python
"""EMA-stack mode table: M15 + H1 specs."""

from __future__ import annotations

import config


def test_modes_present_and_periods():
    names = [m.name for m in config.EMA_STACK_MODES]
    assert names == ["M15", "H1"]
    by = config.EMA_STACK_MODE_BY_NAME
    assert by["M15"].tf == "M15" and by["M15"].periods == (20, 80, 320)
    assert by["H1"].tf == "H1" and by["H1"].periods == (20, 80, 480)


def test_modes_center_is_largest():
    for m in config.EMA_STACK_MODES:
        assert m.periods[2] == max(m.periods)
```

- [ ] **Step 2: Run test to verify it fails**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest tests/test_ema_modes.py -v
```
Expected: FAIL — `AttributeError: module 'config' has no attribute 'EMA_STACK_MODES'`.

- [ ] **Step 3: Implement**

In `config.py`, immediately AFTER the existing `EMA_STACK_HISTORY_BARS` line (~172), add:

```python

# Multi-mode oscillator (M15 + 1H). Each mode is a single-series EMA stack with
# the same fast=EMA20 / mid=EMA20x4 / center=EMA20x(next-TF-multiple) shape:
#   M15: 20/80/320  (EMA80~H1, EMA320~H4)   center=H4
#   H1 : 20/80/480  (EMA80~4H, EMA480~D1)   center=D1
# bars: live fetch deep enough for the center EMA (~4.7x period); display ~480
# trailing bars; history = broker practical depth.
@dataclass(frozen=True)
class EmaStackMode:
    name: str
    tf: str                       # MT5 timeframe label (TIMEFRAME_BY_LABEL key)
    periods: tuple[int, int, int]  # fast, mid, center
    fetch_bars: int
    display_bars: int
    history_fetch_bars: int
    history_bars: int


EMA_STACK_MODES: Final[tuple[EmaStackMode, ...]] = (
    EmaStackMode("M15", "M15", (20, 80, 320), 1500, 480, 20000, 20000),
    EmaStackMode("H1",  "H1",  (20, 80, 480), 3000, 480, 20000, 20000),
)
EMA_STACK_MODE_BY_NAME: Final[dict[str, EmaStackMode]] = {
    m.name: m for m in EMA_STACK_MODES
}
```

(`dataclass` + `Final` are already imported in config.py.)

- [ ] **Step 4: Run test to verify it passes**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest tests/test_ema_modes.py -v
```
Expected: PASS (2 passed).

- [ ] **Step 5: Commit (go サインまで実行しない)**

```bash
git add config.py tests/test_ema_modes.py
git commit -m "feat(ema): add M15/H1 oscillator mode table

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: disparity_bands を `modes` 構造化 + H1 バンド生成

**Files:** Modify `analyzer/disparity_bands.py`, `scripts/gen_ema_disparity_bands.py`; Create `data/ema_disparity_bands.json`(regen); Modify `tests/test_disparity_bands.py`, `tests/test_ema_disparity_bands_artifact.py`

- [ ] **Step 1: Write the failing tests**

In `tests/test_disparity_bands.py`, replace `test_load_bands_valid` and add a mode test:

```python
def test_load_bands_valid(tmp_path):
    p = tmp_path / "b.json"
    p.write_text(json.dumps({"modes": {
        "M15": {"bands": {"ema20": {"pos": {}, "neg": {}}}},
        "H1":  {"bands": {"ema480": {"pos": {}, "neg": {}}}},
    }}), encoding="utf-8")
    assert load_bands("M15", path=p) == {"ema20": {"pos": {}, "neg": {}}}
    assert load_bands("H1", path=p) == {"ema480": {"pos": {}, "neg": {}}}


def test_load_bands_unknown_mode(tmp_path):
    p = tmp_path / "b.json"
    p.write_text(json.dumps({"modes": {"M15": {"bands": {}}}}), encoding="utf-8")
    assert load_bands("H1", path=p) is None
```

Update the existing `test_load_bands_missing` / `test_load_bands_malformed` calls to pass a mode:

```python
def test_load_bands_missing(tmp_path):
    assert load_bands("M15", path=tmp_path / "nope.json") is None


def test_load_bands_malformed(tmp_path):
    p = tmp_path / "bad.json"
    p.write_text("{not json", encoding="utf-8")
    assert load_bands("M15", path=p) is None
```

In `tests/test_ema_disparity_bands_artifact.py`, replace the body with the `modes` schema:

```python
def test_committed_bands_artifact_schema():
    path = config.PROJECT_ROOT / "data" / "ema_disparity_bands.json"
    assert path.exists(), "run scripts/gen_ema_disparity_bands.py first"
    doc = json.loads(path.read_text(encoding="utf-8"))
    modes = doc["modes"]
    assert set(modes) == {"M15", "H1"}
    expected = {"M15": (20, 80, 320), "H1": (20, 80, 480)}
    for name, periods in expected.items():
        m = modes[name]
        assert tuple(m["periods"]) == periods
        bands = m["bands"]
        assert set(bands) == {f"ema{p}" for p in periods}
        for key in bands:
            for side in ("pos", "neg"):
                s = bands[key][side]
                assert set(s) == {"p90", "p95", "p99", "max", "n"}
                assert s["p90"] <= s["p95"] <= s["p99"] <= s["max"]
                assert s["n"] > 0
```

- [ ] **Step 2: Run tests to verify they fail**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest tests/test_disparity_bands.py tests/test_ema_disparity_bands_artifact.py -v
```
Expected: FAIL — `load_bands()` signature mismatch (positional mode) / artifact lacks `modes`.

- [ ] **Step 3: Implement `load_bands(mode, *, path)`**

In `analyzer/disparity_bands.py`, replace `_read`, the cache globals, and `load_bands` with:

```python
def _read_doc(path: Path) -> dict | None:
    try:
        with open(path, encoding="utf-8") as fh:
            return json.load(fh)
    except (OSError, ValueError):
        return None


_cached_doc: dict | None = None
_cached_done = False


def load_bands(mode: str = "M15", *, path: Path | None = None) -> dict | None:
    """Return the disparity bands for *mode* ("M15"/"H1"), or None.

    Reads data/ema_disparity_bands.json (cached for the default path). An
    explicit *path* (tests) bypasses the cache."""
    if path is not None:
        doc = _read_doc(path)
    else:
        global _cached_doc, _cached_done
        if not _cached_done:
            _cached_doc = _read_doc(
                config.PROJECT_ROOT / "data" / "ema_disparity_bands.json")
            _cached_done = True
        doc = _cached_doc
    if not isinstance(doc, dict):
        return None
    entry = (doc.get("modes") or {}).get(mode)
    if not isinstance(entry, dict):
        return None
    bands = entry.get("bands")
    return bands if isinstance(bands, dict) else None
```

- [ ] **Step 4: Implement the generator (both modes)**

In `scripts/gen_ema_disparity_bands.py`, replace `DEFAULT_CSV`/`main` with a per-mode build:

```python
M15_CSV = config.PROJECT_ROOT / "XAUUSD_15 Mins_Bid_2010.01.01_2025.12.31.csv"
H1_CSV = config.PROJECT_ROOT / "XAUUSD_Hourly_Bid_2010.01.01_2025.12.31.csv"
OUT_PATH = config.PROJECT_ROOT / "data" / "ema_disparity_bands.json"

# (mode name, csv path) pulled from the mode table for periods.
_SOURCES = {"M15": M15_CSV, "H1": H1_CSV}


def main(argv: list[str]) -> int:
    modes_out = {}
    for spec in config.EMA_STACK_MODES:
        csv_path = _SOURCES[spec.name]
        if not csv_path.exists():
            print("ERROR: CSV not found: %s" % csv_path)
            return 1
        closes = read_dukascopy_closes(csv_path)
        bands = compute_bands(closes, periods=spec.periods)
        modes_out[spec.name] = {
            "tf": spec.tf, "periods": list(spec.periods),
            "source": csv_path.name, "bands": bands,
        }
        print("OK %s (bars=%d): " % (spec.name, closes.size) + ", ".join(
            "%s p99 pos=%.2f neg=%.2f" % (k, b["pos"]["p99"], b["neg"]["p99"])
            for k, b in bands.items()))
    doc = {
        "generated_at": datetime.now(timezone.utc).isoformat(timespec="seconds"),
        "modes": modes_out,
    }
    OUT_PATH.write_text(json.dumps(doc, indent=2), encoding="utf-8")
    print("OK wrote %s" % OUT_PATH)
    return 0
```

(Keep the existing imports + `read_dukascopy_closes`/`compute_bands` import. Remove the now-unused single-CSV constants.)

- [ ] **Step 5: Run the generator to regenerate the JSON**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe scripts/gen_ema_disparity_bands.py
```
Expected: `OK M15 (bars=~380000)...`, `OK H1 (bars=~95000)...`, `OK wrote .../ema_disparity_bands.json`. Sanity: H1 ema480 p99 はオーダー数% で M15 より大きめ。

- [ ] **Step 6: Run tests to verify they pass**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest tests/test_disparity_bands.py tests/test_ema_disparity_bands_artifact.py -v
```
Expected: PASS.

- [ ] **Step 7: Commit (go サインまで実行しない)**

```bash
git add analyzer/disparity_bands.py scripts/gen_ema_disparity_bands.py data/ema_disparity_bands.json tests/test_disparity_bands.py tests/test_ema_disparity_bands_artifact.py
git commit -m "feat(disparity): per-mode (M15/H1) disparity bands

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: ema_stack をモード引数化

**Files:** Modify `analyzer/ema_stack.py`; Modify `tests/test_ema_stack.py`

- [ ] **Step 1: Write the failing test (append to tests/test_ema_stack.py)**

```python
def _ramp_df_n(n: int):
    import numpy as np, pandas as pd
    idx = pd.date_range("2026-01-01", periods=n, freq="1h", tz="UTC")
    close = 2000.0 + 0.2 * np.arange(n, dtype=float)
    return pd.DataFrame({"open": close, "high": close + 1,
                         "low": close - 1, "close": close}, index=idx)


def test_compute_ema_stack_for_h1_mode():
    import config
    from analyzer.ema_stack import compute_ema_stack_for
    spec = config.EMA_STACK_MODE_BY_NAME["H1"]
    conn = _StubConnector({"XAUUSD": "XAUUSD"}, _ramp_df_n(3000))
    snap = compute_ema_stack_for(conn, spec)
    assert snap.stale is False
    assert snap.mode == "H1"
    assert snap.periods == (20, 80, 480)
    assert len(snap.dev_price) == spec.display_bars


def test_compute_ema_stack_default_is_m15():
    snap = compute_ema_stack(_StubConnector({"XAUUSD": "XAUUSD"}, _ramp_df(1500)))
    assert snap.mode == "M15" and snap.periods == (20, 80, 320)
```

- [ ] **Step 2: Run to verify fail**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest tests/test_ema_stack.py::test_compute_ema_stack_for_h1_mode tests/test_ema_stack.py::test_compute_ema_stack_default_is_m15 -v
```
Expected: FAIL — `compute_ema_stack_for` undefined / `mode` attribute missing.

- [ ] **Step 3: Implement**

In `analyzer/ema_stack.py`:

(a) Add `mode` to the dataclass (after `bands`):
```python
    bands: dict | None = None          # disparity percentile bands (feature 1)
    mode: str = "M15"                  # oscillator mode name (M15 / H1)
```

(b) Replace `_stale_snapshot`:
```python
def _stale_snapshot(symbol: str | None, *, periods=config.EMA_STACK_PERIODS,
                    mode: str = "M15") -> EmaStackSnapshot:
    return EmaStackSnapshot(
        symbol=symbol, periods=periods,
        price=None, ema_fast=None, ema_mid=None, ema_center=None,
        times_ms=(), dev_price=(), dev_fast=(), dev_mid=(),
        as_of=time.time(), stale=True, bands=load_bands(mode), mode=mode,
    )
```

(c) Replace `_ema_tf_const` with a tf-label resolver:
```python
def _tf_const(tf_label: str) -> int:
    spec = config.TIMEFRAME_BY_LABEL.get(tf_label)
    return spec.mt5_const if spec is not None else mt5.TIMEFRAME_M15
```

(d) Re-signature `compute_ema_stack` and use the params (replace the `sym`/`df`/`p_fast..`/`return` lines accordingly):
```python
def compute_ema_stack(
    connector,
    *,
    tf: str = config.EMA_STACK_TF,
    periods: tuple[int, int, int] = config.EMA_STACK_PERIODS,
    fetch_bars: int = config.EMA_STACK_FETCH_BARS,
    display_bars: int = config.EMA_STACK_DISPLAY_BARS,
    mode: str = "M15",
) -> EmaStackSnapshot:
    sym = config.SYMBOLS[0].base
    if sym not in connector.resolved_symbols:
        return _stale_snapshot(None, periods=periods, mode=mode)
    try:
        df = connector.copy_rates(sym, _tf_const(tf), fetch_bars)
    except Exception:  # noqa: BLE001
        log.exception("compute_ema_stack: copy_rates failed for %s", sym)
        return _stale_snapshot(sym, periods=periods, mode=mode)
    if df is None or df.empty or "close" not in df.columns:
        return _stale_snapshot(sym, periods=periods, mode=mode)
    closed = df.iloc[:-1]
    closes = closed["close"].to_numpy(dtype=float)
    p_fast, p_mid, p_center = periods
    if closes.size < p_center:
        return _stale_snapshot(sym, periods=periods, mode=mode)
    ema_f = _ema(closes, p_fast)
    ema_m = _ema(closes, p_mid)
    ema_c = _ema(closes, p_center)
    times_full = closed.index.values.astype("datetime64[ns]").astype("int64") // 1_000_000
    n = display_bars
    sl = slice(-n, None)
    c = ema_c[sl]

    def _dev(arr: np.ndarray) -> tuple[float, ...]:
        return tuple(float((x - y) / y * 100.0) if y else 0.0
                     for x, y in zip(arr[sl], c))

    return EmaStackSnapshot(
        symbol=sym, periods=periods,
        price=float(closes[-1]), ema_fast=float(ema_f[-1]),
        ema_mid=float(ema_m[-1]), ema_center=float(ema_c[-1]),
        times_ms=tuple(int(t) for t in times_full[sl]),
        dev_price=_dev(closes), dev_fast=_dev(ema_f), dev_mid=_dev(ema_m),
        as_of=time.time(), stale=False, bands=load_bands(mode), mode=mode,
    )


def compute_ema_stack_for(connector, mode_spec, *, deep: bool = False) -> EmaStackSnapshot:
    """Compute one oscillator mode from a config.EmaStackMode spec.

    deep=True uses the larger history fetch/display sizes (for /api/ema_history)."""
    fb = mode_spec.history_fetch_bars if deep else mode_spec.fetch_bars
    db = mode_spec.history_bars if deep else mode_spec.display_bars
    return compute_ema_stack(connector, tf=mode_spec.tf, periods=mode_spec.periods,
                             fetch_bars=fb, display_bars=db, mode=mode_spec.name)
```

- [ ] **Step 4: Run tests (new + existing ema_stack)**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest tests/test_ema_stack.py tests/test_disparity_bands.py -v
```
Expected: PASS. Existing tests call `compute_ema_stack(stub)` (kw-only defaults → M15) — still green. `test_serialize_ema_stack_includes_bands` builds EmaStackSnapshot with explicit kwargs; `mode` defaults "M15" — green.

- [ ] **Step 5: Commit (go サインまで)**

```bash
git add analyzer/ema_stack.py tests/test_ema_stack.py
git commit -m "feat(ema): parameterise compute_ema_stack by mode + compute_ema_stack_for

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: state を mode キー化 + serialize に mode

**Files:** Modify `analyzer/state.py`, `dashboard/serialize.py`; Test `tests/test_state_and_serialize.py`

- [ ] **Step 1: Write the failing test (append to tests/test_state_and_serialize.py)**

```python
def test_state_holds_both_ema_modes():
    from analyzer.state import LatestState
    from analyzer.ema_stack import EmaStackSnapshot

    def _snap(mode, periods):
        return EmaStackSnapshot(
            symbol="XAUUSD", periods=periods, price=1.0, ema_fast=1.0,
            ema_mid=1.0, ema_center=1.0, times_ms=(1,), dev_price=(0.0,),
            dev_fast=(0.0,), dev_mid=(0.0,), as_of=1.0, stale=False, mode=mode)

    st = LatestState()
    st.set_ema_stack(_snap("M15", (20, 80, 320)))
    st.set_ema_stack(_snap("H1", (20, 80, 480)))
    blob = st.snapshot()
    assert blob["ema_stack"].mode == "M15"
    assert blob["ema_stack_h1"].mode == "H1"
```

- [ ] **Step 2: Run to verify fail**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest tests/test_state_and_serialize.py::test_state_holds_both_ema_modes -v
```
Expected: FAIL — `KeyError: 'ema_stack_h1'` (snapshot has no such key) / set_ema_stack overwrote.

- [ ] **Step 3: Implement state**

In `analyzer/state.py`:

(a) Replace the init field (line ~71 `self._ema_stack: Optional[EmaStackSnapshot] = None`):
```python
        self._ema_stacks: dict[str, EmaStackSnapshot] = {}
```

(b) Replace `set_ema_stack` (lines ~166-173):
```python
    def set_ema_stack(self, snapshot: EmaStackSnapshot) -> None:
        """Publish an EMA-stack oscillator snapshot, keyed by its mode
        (M15 / H1). Counts as a heavy domain → next WS push is a full snapshot."""
        with self._cond:
            self._ema_stacks[snapshot.mode] = snapshot
            self._monotonic_version += 1
            self._analysis_version += 1
            self._cond.notify_all()
```

(c) Replace the `ema_stack` property (lines ~237-240):
```python
    @property
    def ema_stacks(self) -> dict[str, EmaStackSnapshot]:
        with self._lock:
            return dict(self._ema_stacks)
```

(d) In `snapshot()` (line ~268), replace `"ema_stack": self._ema_stack,` with:
```python
                "ema_stack": self._ema_stacks.get("M15"),
                "ema_stack_h1": self._ema_stacks.get("H1"),
```

- [ ] **Step 4: Implement serialize (add mode + the h1 key path)**

In `dashboard/serialize.py` `serialize_ema_stack` return dict, add after `"ema_center": ...`:
```python
        "mode": s.mode,
```
In `snapshot_to_json` (line ~578 `"ema_stack": serialize_ema_stack(...)`), add the h1 line after it:
```python
        "ema_stack": serialize_ema_stack(snap.get("ema_stack")),  # type: ignore[arg-type]
        "ema_stack_h1": serialize_ema_stack(snap.get("ema_stack_h1")),  # type: ignore[arg-type]
```

- [ ] **Step 5: Run tests**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest tests/test_state_and_serialize.py tests/test_ema_stack.py -q
```
Expected: PASS (existing serialize_ema_stack shape test still green; `mode` is additive).

- [ ] **Step 6: Commit (go サインまで)**

```bash
git add analyzer/state.py dashboard/serialize.py tests/test_state_and_serialize.py
git commit -m "feat(ema): keep both oscillator modes in state + serialize

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: analysis_loop で両モード計算

**Files:** Modify `analyzer/analysis_loop.py:219-223`

- [ ] **Step 1: Implement (no new unit test — covered by integration Task 8)**

Replace the EMA-stack block (lines ~219-223):
```python
        # EMA-stack oscillator — compute every configured mode (M15 + H1), each
        # guarded independently so one mode failing leaves the other live.
        for _spec in config.EMA_STACK_MODES:
            try:
                self._state.set_ema_stack(
                    ema_stack.compute_ema_stack_for(self._connector, _spec))
            except Exception:  # noqa: BLE001
                log.exception("ema_stack compute failed (%s)", _spec.name)
```

(`config` and `ema_stack` are already imported in analysis_loop.py.)

- [ ] **Step 2: Verify import + full suite still green**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -c "import analyzer.analysis_loop; print('import OK')"
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest -q
```
Expected: import OK; all passed.

- [ ] **Step 3: Commit (go サインまで)**

```bash
git add analyzer/analysis_loop.py
git commit -m "feat(ema): compute both M15 and H1 oscillator modes each cycle

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: /api/ema_history に tf パラメータ

**Files:** Modify `dashboard/lite_server.py:384-401`

- [ ] **Step 1: Implement**

Replace the `get_ema_history` body:
```python
    @app.route("/api/ema_history", methods=["GET"])
    def get_ema_history():
        """Deep EMA-stack history for the oscillator. ?tf=M15|H1 (default M15).

        Computed on demand (frontend fetches per active mode + polls) so the
        full multi-month series never rides the live WS snapshot. Read-only."""
        from analyzer import ema_stack
        from dashboard.serialize import serialize_ema_stack
        if connector is None:
            return jsonify(None), 503
        tf = request.args.get("tf", "M15")
        spec = config.EMA_STACK_MODE_BY_NAME.get(tf) or config.EMA_STACK_MODE_BY_NAME["M15"]
        snap = ema_stack.compute_ema_stack_for(connector, spec, deep=True)
        return jsonify(serialize_ema_stack(snap))
```

(`request` is already imported in lite_server.py — it's used by other routes.)

- [ ] **Step 2: Verify import**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -c "import dashboard.lite_server; print('import OK')"
```
Expected: import OK. (Endpoint exercised live in Task 8.)

- [ ] **Step 3: Commit (go サインまで)**

```bash
git add dashboard/lite_server.py
git commit -m "feat(ema): /api/ema_history accepts ?tf=M15|H1

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: フロント — タブ + 動的ラベル/キー + モード別履歴

**Files:** Modify `static/app.js`, `static/app.css`

- [ ] **Step 1: Per-mode history cache + mode-aware fetch**

In `static/app.js`, replace the `EMA_HISTORY` declaration + `fetchEmaHistory` (lines ~696-702):
```javascript
let EMA_HISTORY = {};                           // per-mode deep history cache
function emaMode() {
    try { return localStorage.getItem('mt5-ematf') || 'M15'; } catch (_e) { return 'M15'; }
}
function fetchEmaHistory(mode) {
    const m = mode || emaMode();
    fetch('/api/ema_history?tf=' + encodeURIComponent(m))
        .then(r => (r.ok ? r.json() : null))
        .then(d => { if (d && d.t && d.t.length > 1) { EMA_HISTORY[m] = d; paintEmaStack(latestSnap); } })
        .catch(() => {});
}
```

- [ ] **Step 2: Mode-aware data source + tab click handler**

In `paintEmaStack`, inside the `if (!el._emaInit)` block, after the `▶ 直近` click listener, add a tab handler:
```javascript
        el.addEventListener('click', ev => {
            const tab = ev.target.closest('.ema-tab[data-ematf]');
            if (!tab) return;
            const m = tab.dataset.ematf;
            try { localStorage.setItem('mt5-ematf', m); } catch (_e) {}
            el._emaStamp = null;                 // force re-render
            if (!EMA_HISTORY[m]) fetchEmaHistory(m);
            paintEmaStack(latestSnap);
        });
```

Replace the data-source line (`const d = EMA_HISTORY || (snap && snap.ema_stack);`, ~line 746) with:
```javascript
    const mode = emaMode();
    const live = mode === 'H1' ? (snap && snap.ema_stack_h1) : (snap && snap.ema_stack);
    const d = EMA_HISTORY[mode] || live;
```

- [ ] **Step 3: Dynamic readout labels + band keys**

In `_emaRender`, replace the readout build (the `spO` definition stays; change the three `.ema-k` value lines + add tabs). Replace the `const read = ...` block through the EMA320 line with:
```javascript
    const P = data.periods || [20, 80, 320];
    const k = (i) => 'ema' + P[i];
    const m = emaMode();
    const tabs = `<span class="ema-tabs">`
        + `<button class="ema-tab${m === 'M15' ? ' on' : ''}" data-ematf="M15">M15</button>`
        + `<button class="ema-tab${m === 'H1' ? ' on' : ''}" data-ematf="H1">1H</button></span>`;
    const read =
        `<div class="ema-read">`
      + tabs
      + `<span class="ema-side ${upCls(d320)}">乖離率</span>`
      + `<span class="ema-k"><i class="ema-dot" style="background:#ffb74d"></i>EMA${P[0]} ${spO(dr(data.ema_fast), k(0))}</span>`
      + `<span class="ema-k"><i class="ema-dot" style="background:#4d8eff"></i>EMA${P[1]} ${spO(dr(data.ema_mid), k(1))}</span>`
      + `<span class="ema-k"><i class="ema-dot ema-dot-center"></i>EMA${P[2]} ${spO(d320, k(2))}</span>`
```
(Leave the trailing `${cnt}本 ...` mute span + `▶ 直近` button lines unchanged.)

- [ ] **Step 4: Dynamic band keys in the hover**

In `_emaHover`, replace the three hover spans (the `emaOxTier(k20, B && B.ema20)` lines) with period-derived keys:
```javascript
        const P = data.periods || [20, 80, 320];
        hov.innerHTML = `<b>${fmtJSTdate(sec)} ${fmtJSTclockNoSec(sec)}</b>`
            + `<span class="${emaOxTier(k20, B && B['ema' + P[0]])}">EMA${P[0]} ${sgn(k20)}%</span>`
            + `<span class="${emaOxTier(k80, B && B['ema' + P[1]])}">EMA${P[1]} ${sgn(k80)}%</span>`
            + `<span class="${emaOxTier(k320, B && B['ema' + P[2]])}">EMA${P[2]} ${sgn(k320)}%</span>`;
```
(The `const B`, `k20/k80/k320` lines just above stay as-is.)

- [ ] **Step 5: Mode-aware initial history fetch**

In the startup code (line ~1865 `fetchEmaHistory();`), change to fetch the active mode:
```javascript
    fetchEmaHistory(emaMode());       // deep EMA history for the active mode
    setInterval(() => fetchEmaHistory(emaMode()), EMA_HISTORY_POLL_MS);
```

- [ ] **Step 6: CSS for tabs (append to static/app.css)**

```css
/* M15 / 1H oscillator mode tabs in the readout. Mono, compact. */
.ema-tabs { display: inline-flex; gap: 3px; margin-right: var(--space-3); }
.ema-tab {
    font-family: var(--mono); font-size: var(--text-xs); font-weight: 600;
    color: var(--fg-2); background: transparent; border: 1px solid var(--border);
    border-radius: 5px; padding: 1px 8px; cursor: pointer; line-height: 1.5;
}
.ema-tab.on { color: var(--bg-0); background: var(--accent); border-color: var(--accent); }
```

- [ ] **Step 7: Syntax check**

```
node --check static/app.js
```
Expected: no output (valid).

- [ ] **Step 8: Commit (go サインまで)**

```bash
git add static/app.js static/app.css
git commit -m "feat(ema): M15/1H mode tabs on the oscillator (dynamic labels/bands)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: 統合検証

**Files:** なし(検証のみ)。feedback_integration_verify 準拠。

- [ ] **Step 1: 全スイート緑**

```
C:/Users/ohuch/AppData/Local/Python/pythoncore-3.14-64/python.exe -m pytest -q
```
Expected: all passed（231 + 新規 ~7）。

- [ ] **Step 2: /api/ema_history?tf=H1 が H1 を返すか**

サーバ再起動後:
```
curl -s "http://127.0.0.1:8050/api/ema_history?tf=H1" | grep -o '"periods": \[[^]]*\]' | head -1
curl -s "http://127.0.0.1:8050/api/ema_history?tf=H1" | grep -o '"mode": "[^"]*"' | head -1
```
Expected: `"periods": [20, 80, 480]`、`"mode": "H1"`。M15(既定)は `[20, 80, 320]`。

- [ ] **Step 3: サーバ再起動 + ブラウザ**

8050 PID kill → `main.py` 起動。`http://127.0.0.1:8050/`:
```
B=~/.claude/skills/gstack/browse/dist/browse
"$B" goto http://127.0.0.1:8050/
"$B" console                                   # エラー0
# M15 タブ(既定)の readout
"$B" js "(()=>[...document.querySelectorAll('.ema-read .ema-k')].map(x=>x.textContent.trim()))()"
# 1H タブに切替
"$B" js "(()=>{const t=document.querySelector('.ema-tab[data-ematf=H1]');t&&t.click();return 'clicked';})()"
# 少し待って readout が EMA480 を表示するか
"$B" js "(()=>document.querySelector('.ema-read').textContent.includes('EMA480'))()"
"$B" screenshot "C:/Users/ohuch/Desktop/MT5_Python/_1h_verify.png"
```
Expected: M15 で EMA20/80/320、1H 切替で **EMA20/80/480** 表示、過伸張着色が H1 バンドで発火、ズーム/hover 動作、console 0。確認後 `_1h_verify.png` 削除。

- [ ] **Step 4: コミット不要(検証のみ)**

---

## Self-Review

**1. Spec coverage:**
- 受入1(config M15/H1, H1=(20,80,480)) → T1。✓
- 受入2(snapshot に両 stack) → T4。✓
- 受入3(JSON modes, H1 ema20/80/480) → T2。✓
- 受入4(タブ切替で系列/ラベル/bands/履歴) → T6(history)+T7(frontend)。✓
- 受入5(1H リペイント無=単一H1系列・確定足・因果EMA) → T3(compute は M15 と同一ロジック、tf=H1/periods のみ差)。✓
- 受入6(pytest 全緑+H1ケース, サーバ稼働) → T1-5 各テスト + T8。✓

**2. Placeholder scan:** TBD/TODO 無し。`<PID>`/`~95000` 等はランタイム/概算値。全コードブロック実コード。✓

**3. Type consistency:** `EmaStackMode(name,tf,periods,fetch_bars,display_bars,history_fetch_bars,history_bars)` を T1 定義→T2生成器/T3 compute_ema_stack_for/T6 lite_server/T5 loop で同フィールド参照。`load_bands(mode, *, path)` を T2 定義→T3 で `load_bands(mode)`。`EmaStackSnapshot.mode` を T3 定義→T4 state(`snapshot.mode`)/serialize(`s.mode`)/test で参照。snapshot キー `ema_stack`/`ema_stack_h1` を T4 emit→T7 frontend(`snap.ema_stack_h1`)で参照。bands キー `ema{period}` を T2 生成→T7 `'ema'+P[i]` で参照。一貫。✓

ギャップ無し。
